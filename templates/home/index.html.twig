<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bienvenue chez TAB Logistique</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            font-family: 'Arial', sans-serif;
        }
        
        .welcome-card {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            padding: 3rem;
            text-align: center;
            margin: 50px auto;
            max-width: 600px;
        }
        
        .logo {
            font-size: 4rem;
            color: #667eea;
            margin-bottom: 1rem;
        }
        
        .welcome-title {
            color: #333;
            font-size: 2.5rem;
            font-weight: bold;
            margin-bottom: 1rem;
        }
        
        .welcome-subtitle {
            color: #666;
            font-size: 1.2rem;
            margin-bottom: 2rem;
        }
        
        .btn-custom {
            background: linear-gradient(45deg, #667eea, #764ba2);
            color: white;
            border: none;
            padding: 12px 30px;
            border-radius: 25px;
            font-size: 1.1rem;
            font-weight: 600;
            text-decoration: none;
            display: inline-block;
            margin: 10px;
            transition: all 0.3s ease;
        }
        
        .btn-custom:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
            color: white;
        }
        
        .features {
            display: flex;
            justify-content: space-around;
            margin-top: 2rem;
            flex-wrap: wrap;
        }
        
        .feature {
            text-align: center;
            margin: 1rem;
            flex: 1;
            min-width: 150px;
        }
        
        .feature-icon {
            font-size: 2rem;
            color: #667eea;
            margin-bottom: 0.5rem;
        }
        
        .feature-text {
            color: #666;
            font-size: 0.9rem;
        }
        
        @media (max-width: 768px) {
            .welcome-card {
                margin: 20px;
                padding: 2rem;
            }
            
            .welcome-title {
                font-size: 2rem;
            }
            
            .logo {
                font-size: 3rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="welcome-card">
            <!-- Logo -->
            <div class="logo">
                <i class="fas fa-shipping-fast"></i>
            </div>
            
            <!-- Titre principal -->
            <h1 class="welcome-title">Bienvenue chez TAB Logistique</h1>
            
            <!-- Sous-titre -->
            <p class="welcome-subtitle">
                Votre solution complète pour la gestion et le suivi de vos expéditions
            </p>
            
            <!-- Boutons d'action -->
            <div class="mt-4">
                <a href="/colis" class="btn-custom">
                    <i class="fas fa-sign-in-alt me-2"></i>
                    Se connecter
                </a>
                <a href="/suivi" class="btn-custom">
                    <i class="fas fa-search me-2"></i>
                    Suivre un colis
                </a>
            </div>
            
            <!-- Fonctionnalités -->
            <div class="features">
                <div class="feature">
                    <div class="feature-icon">
                        <i class="fas fa-box"></i>
                    </div>
                    <div class="feature-text">Gestion des colis</div>
                </div>
                <div class="feature">
                    <div class="feature-icon">
                        <i class="fas fa-map-marker-alt"></i>
                    </div>
                    <div class="feature-text">Suivi en temps réel</div>
                </div>
                <div class="feature">
                    <div class="feature-icon">
                        <i class="fas fa-chart-line"></i>
                    </div>
                    <div class="feature-text">Rapports détaillés</div>
                </div>
            </div>
            
            <!-- Information de contact -->
            <div class="mt-4 pt-3" style="border-top: 1px solid #eee;">
                <small class="text-muted">
                    <i class="fas fa-phone me-1"></i> +33 1 23 45 67 89 |
                    <i class="fas fa-envelope me-1"></i> contact@tablogistique.fr
                </small>
            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>