{# <form method="post" action="{{ path('app_destinataire_delete', {'id': destinataire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ destinataire.id) }}">
    <button class="btn">Delete</button>
</form> #}


<form method="post" action="{{ path('app_destinataire_delete', {'id': destinataire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="inline-block">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ destinataire.id) }}">
    <button class="px-4 py-2 bg-red-600 text-white font-medium rounded-md hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-offset-2 transition-colors">
        Delete
    </button>
</form>