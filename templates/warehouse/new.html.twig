{% extends 'base.html.twig' %}

{% block title %}New Warehouse{% endblock %}

{% block body %}
    <h1>Create new Warehouse</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Save</button>
    {{ form_end(form) }}

    <a href="{{ path('app_warehouse_index') }}" class="btn btn-secondary mt-2">Back to list</a>
{% endblock %}